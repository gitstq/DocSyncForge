# DocSyncForge - 智能文档同步引擎
# 核心功能零依赖，以下为可选增强依赖

# TUI界面增强（可选）
rich>=13.0.0

# CLI框架增强（可选）
typer>=0.9.0

# 异步HTTP请求（可选，用于多平台同步）
aiohttp>=3.8.0
aiofiles>=23.0.0

# Markdown处理（可选）
markdown>=3.4.0

# YAML配置支持（可选）
pyyaml>=6.0
